# Allow downloads
<Files ~ "\.(exe|dmg|zip)$">
    Order allow,deny
    Allow from all
</Files>

# Prevent directory listing
Options -Indexes

# Force download for executables
<FilesMatch "\.(exe|dmg)$">
    Header set Content-Disposition "attachment"
</FilesMatch>
